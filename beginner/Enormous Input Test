
#include <bits/stdc++.h> 

using namespace std;


int main(){
    long long int n,k;
    cin >> n >>k;
    int count = 0;
    while(n--){
        long long int r;
        cin >> r;
        if(r%k == 0)
        count++;
    }
    cout<<count;
}
