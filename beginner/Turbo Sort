#include <iostream>
#include <bits/stdc++.h> 

using namespace std;

int main() {
	// your code goes here
	long long int t;
	cin >> t;
	long long int arr[1000000];
	for(long long int i=0;i<t;i++){
	    cin>>arr[i];
	}
	long long int n = sizeof(arr)/sizeof(arr[0]); 
	sort(arr,arr+n);
	for(long long int i=0;i<t;i++){
	    cout<<arr[i]<<endl;
	}
	return 0;
}
