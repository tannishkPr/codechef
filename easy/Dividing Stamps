#include <iostream>
using namespace std;

int main() {
	    int n;
	    cin>>n;
	    int sum=0;
	    int temp;
	    for(int i=0;i<n;i++){
	        cin>>temp;
	        sum+=temp;
	    }
	    if(sum == (n*(n+1))/2)
	        cout<<"YES";
	    else
	        cout<<"NO";
	   return 0;

	}
